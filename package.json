{
  "name": "syncpad",
  "version": "0.2.0",
  "description": "A desktop-first notes app for Windows with local storage and a future cloud sync path.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/store.js && node --check src/renderer/app.js",
    "smoke-store": "node scripts/smoke-store.js"
  },
  "keywords": [
    "notes",
    "desktop",
    "electron",
    "sync"
  ],
  "author": "koltregaskes",
  "license": "MIT",
  "devDependencies": {
    "electron": "^35.0.0"
  }
}
